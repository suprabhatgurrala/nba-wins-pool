# Example .env for local development
# Copy this file to `.env` and set the real token there. `.env` is listed in .gitignore.

# Discord bot token (keep this secret — do NOT commit the real token)
DISCORD_TOKEN=your-discord-token-here
